{
  "private": "true",
  "name": "marshal",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "linters/*"
  ],
  "devDependencies": {
    "@marshal/eslint-config": "1.0.0",
    "@marshal/prettier-config": "1.0.0",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "lerna": "lerna",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "metro": "yarn workspace @marshal/mobile start",
    "ios": "yarn workspace @marshal/mobile ios",
    "android": "yarn workspace @marshal/mobile android",
    "core:build": "yarn workspace @marshal/core build",
    "core:build:w": "yarn workspace @marshal/core build:w",
    "core:sb": "yarn workspace @marshal/core storybook",
    "web:dev": "yarn workspace @marshal/web dev"
  },
  "eslintConfig": {
    "extends": [
      "@marshal/eslint-config"
    ]
  },
  "prettier": "@marshal/prettier-config"
}